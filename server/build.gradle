apply plugin: 'application'

mainClassName = 'com.server.main.Server'

jar {
    from {
        configurations.compile.collect{ it.isDirectory() ? it : zipTree(it)}
    }

    manifest {
        attributes(
                'Main-Class': 'com.server.main.Server'
        )
    }
}